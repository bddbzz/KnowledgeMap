//完成如下函数，实现整数数值运算功能

//基本要求：
//1.支持+-*/运算
//2.支持()提升优先级
//3.不考虑输入、运算结果在安全整型之外的情况，即数值范围保证在(Number.MIN_SAFE_INTEGER ~ Number.MAX_SAFE_INTEGER)之间

//示例：
//如source='3*3+4*4' 则result为25
//如source='(1+1)*2' 则result为4

function calculate(source) {
    let result = null
    //处理逻辑
    return result
}
